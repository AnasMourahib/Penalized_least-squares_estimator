% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validation.R
\name{cross_validation}
\alias{cross_validation}
\title{K-Fold Cross-Validation Score for Penalized Estimation}
\usage{
cross_validation(
  d,
  r,
  A,
  grid,
  lambda,
  num_col = NULL,
  start,
  type = c("SSR_row_HR", "SSR_row_log"),
  p,
  w,
  num_class = 10
)
}
\arguments{
\item{d}{Integer. Dimension of the problem (number of variables).}

\item{r}{Integer. Number of columns in the true coefficient matrix \code{A}.}

\item{A}{Numeric matrix. The true coefficient matrix.}

\item{grid}{Numeric matrix. Evaluation grid, where each row is a point \eqn{c_m}.}

\item{lambda}{Numeric. Penalization parameter for the estimation procedure.}

\item{num_col}{Integer (optional). Number of columns (i.e., number of extreme directions) to be used in the estimation. Defaults to \code{r}.}

\item{start}{Numeric vector. A \code{d × num_col} vectorized starting value for the optimization procedure. The rows of the coefficient matrix should be stacked sequentially.}

\item{type}{Character string. Specifies the fitted model type: either \code{"SSR_row_log"} for the logistic mixture model or \code{"SSR_row_HR"} for the Hüsler--Reiss mixture model.}

\item{p}{Numeric. Penalization exponent.}

\item{w}{A list containing two sub-lists: \code{w$train} and \code{w$test}. Each sub-list contains \code{num_class} elements (typically 10), each of which is a numeric vector of length \code{q} (number of evaluation points). For fold \code{i}, \code{w$train[[i]]} contains empirical stdf estimates for the q points based on the training set \eqn{S \setminus S_i}, while \code{w$test[[i]]} contains those based on the validation set \eqn{S_i}.}

\item{num_class}{Integer. Number of cross-validation folds. Default is 10.}
}
\value{
A numeric scalar: the mean cross-validation score computed across all folds, as described in Algorithm 2 from Mourahib, Anas, Anna Kiriliouk, and Johan Segers (2025), \emph{A penalized least squares estimator for extreme-value mixture models}, arXiv preprint arXiv:2506.15272.
}
\description{
Computes the K=10 cross-validation score corresponding to a given penalization parameter \code{lambda}, based on Algorithm 2 from Mourahib, Kiriliouk, and Segers (2025).
The function evaluates the model fit by repeatedly estimating the penalized least squares estimator on training folds and computing prediction error on the corresponding test folds.
}
\references{
Mourahib, A., Kiriliouk, A., & Segers, J. (2025). A penalized least squares estimator for extreme-value mixture models. arXiv preprint arXiv:2506.15272.
}
