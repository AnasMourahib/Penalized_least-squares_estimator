% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Help_functions.R
\name{W_calculus}
\alias{W_calculus}
\title{Compute Empirical Stable Tail Dependence Functions for Cross-Validation}
\usage{
W_calculus(k, num_class, X, grid, q)
}
\arguments{
\item{k}{The order statistic threshold, typically set to \code{floor(n^\alpha)} where \code{n} is sample size.}

\item{num_class}{Number of folds (classes) to split the data into for cross-validation (e.g., 10).}

\item{X}{A numeric matrix of dimension \code{n x d}, containing the observed multivariate data.}

\item{grid}{A matrix of evaluation points \eqn{c_1, \ldots, c_q}, where each row corresponds to a point in the unit simplex.}

\item{q}{The number of evaluation points (i.e., the number of rows in \code{grid}).}
}
\value{
A list with two elements:
\describe{
\item{\code{train}}{A list of length \code{num_class}, each containing a vector of length \code{q} with STDF estimates using training data.}
\item{\code{test}}{A list of length \code{num_class}, each containing a vector of length \code{q} with STDF estimates using test data.}
}
}
\description{
Splits the dataset into \code{num_class} folds and computes the empirical stable tail dependence function
(STDF) estimates using the \code{stdfEmp} function for each training and testing set. Typically used
in a K-fold cross-validation setting to prepare input for penalized model fitting.
}
\seealso{
\code{\link{stdfEmp}} for computing empirical STDF values.
}
